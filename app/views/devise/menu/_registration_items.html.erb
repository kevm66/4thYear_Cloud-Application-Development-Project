<% if user_signed_in? %>
    <%= link_to('Account Settings', edit_user_registration_path, :class => "mdl-navigation__link") %>
<% else %>
    <%= link_to('Register', new_user_registration_path, :class => "mdl-navigation__link") %>
<% end %>
